//unit tests for CRUD functionality

// testing to see if jest is working properly

it('Testing jest, test pass', () => {
    expect(1).toBe(1)
})

it('Testing jest, test fail', () => {
    expect(1).toBe(2)
})